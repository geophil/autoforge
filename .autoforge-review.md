# End-to-End Review: Execution Phase Visualisation

Date: 2026-05-11  
Branch: autoforge/e23b7547-fa9d-406b-b999-d883d2501fe9  
Test run: `bun test` — **565 pass, 0 fail**

---

## Test Suite

| Criterion | Result | Note |
|-----------|--------|------|
| `bun test` reports all tests passing | **PASS** | 565 pass, 0 fail across 77 files |
| New `subtask_started` event assertions pass | **PASS** | `tests/unit/subtask-started.test.ts` — both tests pass: ordering, payload fields (subtaskId, iteration, sequence, agentType) |
| Shadow payload `subtask_id` + `iteration` assertions pass | **PASS** | Second test in `subtask-started.test.ts` verifies shadow events carry `subtask_id` and `iteration` matching the surrounding subtask |
| Pre-existing tests unaffected (no regression) | **PASS** | All 563 prior tests continue to pass |

---

## (a) Execution Progress Header — STANDARD task (reviewPlan: false)

| Criterion | Result | Note |
|-----------|--------|------|
| Header renders for executing / reviewing / reworking / documenting states only | **PASS** | `EXECUTION_STATES` set used as guard at `dashboard.js:555` |
| Initial paint shows graceful placeholders (before events load) | **PASS** | `renderExecProgressContent(task, [])` called at initial render; shows "Loading active subtask…" and "— of N subtasks done" placeholders |
| State badge rendered | **PASS** | `stateBadge` in `renderExecProgressContent` at `dashboard.js:697` |
| `Iteration N of MAX` shown | **PASS** | Uses `REWORK_MAX = 3` constant; `dashboard.js:698` |
| `X of Y subtasks done in iteration N` | **PASS** | Computed via `computeExecProgressInfo`; modern path uses `subtask_started` iteration field |
| Active subtask: sequence, agent type, description (≤80 chars), elapsed | **PASS** | Rendered at `dashboard.js:712–718`; description truncated at 80 chars |
| Cumulative input/output tokens + estimated cost | **PASS** | Reuses `summarizeTokenUsage`; `dashboard.js:725–731` |
| Header re-populates after `loadEvents` via stable DOM element | **PASS** | `updateExecProgressHeader(events)` called from `loadEvents` at `dashboard.js:915`; stable `id="exec-progress-header"` element |

---

## (b) Per-Iteration History Strip (rework scenario)

| Criterion | Result | Note |
|-----------|--------|------|
| One dot per iteration in which the subtask ran | **PASS** | History entries keyed by iteration in `subtaskRuns[subtaskId]`; `dashboard.js:1134` |
| Dot colour: success=green, concerns=amber, failure=red, running=pulse | **PASS** | CSS classes `--success`, `--concerns`, `--failure`, `--running`; pulse animation in `styles.css` |
| Two dots shown after reviewer rework (iterations 0 and 1) | **PASS** | Two distinct `subtask_started`/`subtask_done` iterations recorded and rendered |
| Findings badge: CRITICAL/MAJOR (red) vs MINOR/NITPICK (yellow) counts | **PASS** | `subtask-findings-count--high` / `--low` applied at `dashboard.js:1176–1182` |
| Findings grouped per iteration (not per subtask) | **PASS** | `reviewByIteration[iter]` indexing at `dashboard.js:979–994`; each dot's panel scoped to `iter` |
| Clicking dot expands inline findings panel | **PASS** | `toggleFindingsPanel` wired via `onclick` at `dashboard.js:1212`; toggles `.hidden` and `--expanded` class |
| Findings panel shows severity, title, location | **PASS** | `subtask-finding-item` template at `dashboard.js:1191–1198` |

---

## (c) Shadow Variants Strip

| Criterion | Result | Note |
|-----------|--------|------|
| Strip hidden when no shadow events for a subtask | **PASS** | `renderShadowVariantsStrip` returns `""` when `shadowEvents.length === 0` |
| Strip appears for subtask with shadow events | **PASS** | `buildShadowRunsPerSubtask` attaches events; `shadowWrapEl.innerHTML` populated at `dashboard.js:1229` |
| Shows candidate id (last 8 chars) | **PASS** | `shortId = variantId.slice(-8)` at `dashboard.js:1275` |
| Shows executor | **FAIL** | `dashboard.js:1276` reads `p.candidate_executor ?? p.executor`, but `shadow.ts` emits `candidate_executor_used`. Executor will always display "—". |
| Shows signed score delta (+ green / − red) | **PASS** | Sign and `shadow-delta--positive/negative` CSS classes applied at `dashboard.js:1284–1286` |
| Shows lessons injected count | **PASS** | `p.candidate_lessons_injected ?? 0` at `dashboard.js:1291` — field name matches `shadow.ts` payload |
| Shows error badge when error present | **PASS** | `shadow-error-badge` rendered conditionally at `dashboard.js:1292` |
| Strip is collapsed by default | **PASS** | Outer `<details class="subtask-shadow-strip">` is not pre-opened |
| "View all" link appears when > 20 entries | **PASS** | `hasMore` guard at `dashboard.js:1328–1330` |

---

## (d) Legacy Fallback (no `subtask_started` events, no `payload.subtask_id` on shadow events)

| Criterion | Result | Note |
|-----------|--------|------|
| Sensible pending/running/done status for subtask cards | **PASS** | Legacy path at `dashboard.js:1053–1060`: first non-done subtask gets `"running"` when `RUNNING_STATES.has(task.state)` |
| Legacy shadow events attached via preceding `subtask_done` | **PASS** | `buildShadowRunsPerSubtask` uses `lastSubtaskDoneId` fallback at `dashboard.js:1249` |
| No JS console errors in legacy mode | **PASS** | All null checks in place; `??.` guards throughout event payload access |
| Shadow strip renders for legacy subtasks with `subtask_done.payload.subtaskId` | **PASS** | `subtaskRuns` populated from `subtask_done` events; `iters.length > 0` so early return at `dashboard.js:1131` is not triggered |
| Latent bug: early `return` at `dashboard.js:1131` skips shadow strip if subtask has no `subtask_started` AND no `subtask_done.payload.subtaskId` | **N-A** | In this edge case, `buildShadowRunsPerSubtask` also produces no entries (since `lastSubtaskDoneId` cannot be set), so shadow strip would correctly show nothing regardless |

---

## (e) Pipeline Event Log (details element + filter)

| Criterion | Result | Note |
|-----------|--------|------|
| Section wrapped in `<details class="event-log-details">` labelled "Event log (forensics)" | **PASS** | `dashboard.js:601–605` |
| Collapsed by default for executing / reviewing / reworking / documenting | **PASS** | `open` attribute omitted for these states |
| Open by default for completed / failed / awaiting_intervention | **PASS** | `["completed", "failed", "awaiting_intervention"].includes(task.state)` at `dashboard.js:601` |
| `#cost-summary` visible above filter (not inside collapsed details) | **PASS** | `#cost-summary` is a sibling ABOVE the `<details>` element at `dashboard.js:600` |
| Filter input present above findings list | **PASS** | `<input type="text" id="event-log-filter">` at `dashboard.js:603` |
| Filter debounced at ~80ms | **PASS** | `setTimeout(() => {...}, 80)` in `wireEventLogFilter` at `dashboard.js:928` |
| Filter matches type and agent (case-insensitive substring) | **PASS** | `data-type` / `data-agent` attributes, `.toLowerCase().includes(query)` at `dashboard.js:935–937` |
| Per-row rendering preserved (dot colour, agent colour, elapsed, tokens, failure badges, rejection categories, restart link, transcript click-to-open) | **PASS** | All existing fields retained in `loadEvents` template at `dashboard.js:867–903` |

---

## Approve / Critique / Intervention / Steering UI Smoke

| Criterion | Result | Note |
|-----------|--------|------|
| Approve / reject buttons unaffected by changes | **PASS** | `approveTask`, `rejectTask` functions intact; no surrounding code modified |
| Critique input (plan + spec) unaffected | **PASS** | `wireCritiqueInput` / `wireSpecCritiqueInput` at `dashboard.js:753–795` unchanged |
| Steering and intervention UIs unaffected | **PASS** | No modifications to intervention/steering path code |

---

## Summary

**Tests:** All 565 pass.

**One MAJOR defect found:**

> `dashboard.js:1276` — `candidate_executor` field read vs `candidate_executor_used` field written by `shadow.ts`. The executor column in the shadow variants strip will always render "—" instead of the actual executor value. Needs a one-line fix: `p.candidate_executor_used ?? p.candidate_executor ?? p.executor ?? null`.

All other acceptance criteria pass code review. No regressions detected.
